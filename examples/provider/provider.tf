terraform {
  required_providers {
    epilot-automation = {
      source  = "epilot-dev/epilot-automation"
      version = "0.0.3"
    }
  }
}

provider "epilot-automation" {
  # Configuration options
}