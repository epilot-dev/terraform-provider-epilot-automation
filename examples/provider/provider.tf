terraform {
  required_providers {
    epilot-automation = {
      source  = "epilot-dev/epilot-automation"
      version = "0.9.0"
    }
  }
}

provider "epilot-automation" {
  # Configuration options
}